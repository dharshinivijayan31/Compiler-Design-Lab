PROCEDURE:

1. Create a FLEX file (optimize.l) to identify identifiers, numbers, operators, and delimiters.

2. Create a BISON file (optimize.y) to define grammar rules for assignment statements and arithmetic expressions.

3. Implement semantic actions to perform optimization during parsing.

4. Apply Constant Folding by evaluating arithmetic operations involving only numeric constants.

5. Apply Algebraic Simplification using rules such as:
   a) x + 0 → x
   b) 0 + x → x
   c) x - 0 → x
   d) x * 1 → x
   e) x / 1 → x

6. Apply Strength Reduction by replacing multiplication by 2 with addition:
   x * 2 → x + x

7. Generate optimized expressions during parsing.

8. Compile the FLEX program using:
   flex optimize.l

9. Compile the BISON program using:
   bison -d optimize.y

10. Generate the executable using:
    gcc lex.yy.c optimize.tab.c -o optimize -lfl

11. Execute the program:
    ./optimize

12. Enter three-address code assignment statements ending with ';'.

13. Observe the optimized output generated by the program.

14. Test various expressions to verify Constant Folding, Algebraic Simplification, and Strength Reduction.

15. Terminate the program after verification.